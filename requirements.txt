python-socketio
websockets
requests
assemblyai

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6  # Required for FastAPI form data and file uploads

# Vector database
qdrant-client>=1.7.0

# Encryption (for HIPAA compliance)
cryptography>=41.0.0

# ML/AI libraries
# Note: PyTorch CPU-only version is installed in Dockerfile to reduce image size
# For local development, install full torch: pip install torch
# IMPORTANT: transformers 4.35.0+ requires PyTorch >= 2.3.0
# For PyTorch 2.2.0 compatibility, use transformers==4.34.0 (pinned in Dockerfile)
transformers>=4.35.0
sentence-transformers>=2.2.0
# torch>=2.1.0  # Installed separately in Dockerfile as CPU-only version

# Document generation
reportlab>=4.0.0
python-docx>=1.1.0
PyPDF2>=3.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Deepgram SDK
deepgram-sdk>=5.3.0

# Environment management
python-dotenv>=1.0.0

# LLM - Google Gemini
google-generativeai>=0.3.0

# Optional medical standards (for future HL7/FHIR support)
# hl7>=1.3.7
# fhir.resources>=7.0.0

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0