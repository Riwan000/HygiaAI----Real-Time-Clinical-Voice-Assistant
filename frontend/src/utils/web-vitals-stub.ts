/**
 * Web Vitals Stub Module
 * 
 * This stub module is used when web-vitals is not installed.
 * It provides empty implementations to prevent import errors.
 * 
 * When web-vitals is installed, Vite will resolve to the actual module.
 * When not installed, these stubs prevent runtime errors.
 */

// Stub implementations - these will be replaced if web-vitals is installed
export const onCLS = () => {
  // No-op: web-vitals not installed
};

export const onFID = () => {
  // No-op: web-vitals not installed
};

export const onFCP = () => {
  // No-op: web-vitals not installed
};

export const onLCP = () => {
  // No-op: web-vitals not installed
};

export const onTTFB = () => {
  // No-op: web-vitals not installed
};

export const onINP = () => {
  // No-op: web-vitals not installed
};

